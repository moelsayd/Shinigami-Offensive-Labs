⚔️ Room 1 – Web Enumeration (Web Recon & Enum)

· يوجد سيرفر ويب يشتغل على http://localhost:8000 (أو 8080).
· يوجد "دومين" (عنوان IP/منفذ) تستهدفُه بالمتصفح أو curl أو gobuster.
· الهدف: تفحص خدمة ويب خارجية (الموقع) لتجد مساراً مخفياً وعلماً.
· الفكرة: تفحص الكود المصدري (HTML) للصفحة الرئيسية، تجد تعليقاً يرشدك لملف secret.html.
· بعد الوصول للملف المخفي، تقرأ العلم وترسله.
· العلم: `THM{web_enum_done}` – قيمة النقاط: +100

# ---
Room 2 – Service Fingerprinting + Hidden Functionality (Easy+)

· خدمة TCP بسيطة على `127.0.0.1:9001` تحمل راية `Welcome to backup service v1.3`.
· ليست HTTP، يجب استخدام nc للتفاعل.
· بكتابة HELP تظهر أوامر مخفية، منها GET FLAG الذي يعرض العلم مباشرة.
· المهارة: مسح الخدمات، التعامل مع بروتوكولات غير قياسية، اكتشاف الوظائف المخفية.
· العلم: `THM{service_fingerprint_basic}` – قيمة النقاط: +200

# ---
🕵️ Room 3 – Hidden Directory (Directory Brute‑forcing)

· يوجد سيرفر ويب على `http://localhost:9000`.
· لا يوجد أي تلميح ظاهر في الموقع. يجب عليك استخدام أداة مثل `gobuster` أو `dirb` لمسح المسارات.
· الهدف: العثور على مجلد مخفي (مثلاً `/admin/`) يحتوي على ملف العلم.
· بعد اكتشاف المجلد، ستجد `flag.txt` بداخله.
· الفلاج: `THM{hidden_dir}` – قيمة النقاط: +150

# ---
🔑 Room 4 – Weak Login (Default Credentials)

· يوجد تطبيق ويب ديناميكي على `http://localhost:7000`، يقدم صفحة دخول (Login).
· التطبيق مكتوب بـ Python (SimpleHTTPRequestHandler)، يقبل بيانات POST.
· بيانات الدخول الضعيفة هي: `admin` / `admin`.
· عند إدخالها بنجاح عبر المتصفح أو curl، يظهر العلم مباشرة.
· الهدف: تخمين كلمة المرور الضعيفة، مما يعكس خطورة استخدام بيانات الاعتماد الافتراضية.
· العلم: `THM{weak_creds}` – قيمة النقاط: +150

# ---
📁 Room 5 – Info Leakage (Backup File Exposure)

· يوجد خادم ويب بسيط على `http://localhost:5000` يخدم ملفات ثابتة.
· لا توجد صفحة تسجيل دخول، بل ملف نسخ احتياطي `backup.zip` ظاهر على السيرفر.
· يمكن تحميل الملف مباشرة (عبر wget أو curl)، ثم فك ضغطه (بدون كلمة مرور).
· يحتوي الأرشيف على ملف `flag.txt` الذي يحمل العلم.
· المهارة المتعلمة: فكرة فحص الملفات المكشوفة (backup files) وتعداد الأصول بشمولية.
· العلم: `THM{info_leak_flag}` – قيمة النقاط: +150

# ---
🤖 Room 6 – Robots.txt Discovery (Basic Web Recon)

· يوجد خادم ويب ثابت على `http://localhost:6000`.
· ملف `robots.txt` متاح ويكشف عن مسار محظور: `/admin/`.
· بزيارة ذلك المسار نجد الملف `flag.txt` الذي يحوي العلم.
· المهارة: فحص الملفات العامة، استطلاع أولي دون أدوات متقدمة.
· العلم: `THM{r0b0ts_txt_l34k}` – قيمة النقاط: +100

# ---
💉 Room 7 – Command Injection (Simple TCP Service)

· يوجد خادم TCP ضعيف على `127.0.0.1:7777` ينفذ أي أمر يُرسل له.
· الاتصال عبر `nc 127.0.0.1 7777` واستعمال `cat flag.txt` يكشف العلم.
· المهارة: أساسيات command injection، استغلال خدمات شبكية غير آمنة.
· العلم: `THM{cmd_inj3ct1on}` – قيمة النقاط: +200o

# ---
📂 Room 8 – Open Directory Listing (Misconfigured Server)

· خادم ويب على `http://localhost:8088` يسمح بعرض قائمة الملفات لعدم وجود index.html.
· التصفّح المباشر يكشف وجود `flag.txt` وسط ملفات وهمية.
· المهارة: استغلال Directory Listing، تصفّح يدوي بسيط.
· العلم: `THM{op3n_d1r_l1st1ng}` – قيمة النقاط: +100

# ---
⚡ Room 9 – API Manipulation (Parameter Tampering)

· خدمة API على `http://localhost:9090/user` تستقبل المُدخل `?id=`.
· لا يوجد تحقق من الصلاحيات: تغيير القيمة إلى `1337` يكشف العلم.
· المهارة: استكشاف API، تلاعب بالبارمترات، عدم التحقق من المُدخل.
· العلم: `THM{api_t4mper_1337}` – قيمة النقاط: +150

# ---
🔓 Room 10 – Weak SSH Simulation (Brute‑force Mindset)

· خادم يحاكي SSH على `127.0.0.1:2222`، يتطلب اسم مستخدم وكلمة سر.
· بيانات الدخول الضعيفة: `test` / `1234`.
· الاتصال بـ `nc 127.0.0.1 2222` وإدخال البيانات الصحيحة يكشف العلم.
· المهارة: عقلية تخمين كلمات المرور، التعامل مع خدمات مقلّدة.
· العلم: `THM{ssh_w34k_p4ss}` – قيمة النقاط: +150

# ---
📝 Room 11 – Default Credentials + Hint Leakage (Chained Discovery)

· خادم ويب على `http://localhost:8888`. صفحة رئيسية عادية.
· تعداد المسارات يكشف عن `/dev-notes.txt` يحتوي على كلمة مرور افتراضية `admin123`.
· صفحة `/login` تنفذ تسجيل الدخول؛ باستخدام `admin:admin123` يظهر العلم.
· المهارة: ربط المعلومات (chaining)، enumeration الشامل، استغلال بيانات الدخول الضعيفة.
· العلم: `THM{ch41n3d_1nf0_l34k}` – قيمة النقاط: +150

# ---
📁 Room 12 – Git Exposure (.git Leak)

· خادم ويب على `http://localhost:6060` يضم صفحة عادية.
· تعداد المسارات بـ gobuster يكشف عن مجلد `/.git/` المكشوف.
· بداخله، يحتوي ملف `config` على تعليق يحمل العلم.
· المهارة: اكتشاف الملفات الحساسة المتروكة، فهم مخاطر ترك `.git` معرفاً للعامة.
· العلم: `THM{d0t_g1t_l34k}` – قيمة النقاط: +150

# ---
⚡ Room 13 – Basic Controlled Exploitation (Chained Attack)

· تطبيق ويب ديناميكي على `http://localhost:7778` يضم صفحة تسجيل دخول مخفية.
· بيانات الدخول الضعيفة: `admin` / `password`.
· بعد الدخول، لوحة تحكم تسمح بأوامر محدودة، منها `read` الذي يستطيع قراءة ملفات على الخادم.
· الأمر `read flag.txt` يكشف العلم الموجود خارج مسار الموقع الإفتراضي.
· المهارة: ربط الثغرات (تسجيل دخول ضعيف + وظيفة قراءة غير مقيدة) للحصول على الهدف.
· العلم: `THM{ch41n3d_expl01t}` – قيمة النقاط: +150

# ---
🔍 Room 14 – Backup Extension Hunting

· خادم ويب على `http://localhost:6061` يحتوي على ملفات PHP عادية.
· استخدام gobuster مع تمديدات (`-x php,bak,old,save,~`) يكشف عن نسخ احتياطية مثل `config.php.save`.
· أحد هذه الملفات يحتوي على العلم في تعليق داخلي.
· المهارة: extension fuzzing، اكتشاف النسخ الاحتياطية، البحث في محتوى الملفات.
· العلم: `THM{ext3nsi0n_hunt3r}` – قيمة النقاط: +150

# ---
📋 Room 15 – HTTP Header Information Leak

· خادم ويب على `http://localhost:5050` يعرض صفحة عادية.
· لكنه يُرجع ترويسة مخصصة `X-Flag` تحتوي على العلم.
· يمكن اكتشافها باستخدام `curl -I` أو `curl -v`.
· المهارة: فحص الترويسات، تحليل استجابة الخادم، فهم HTTP.
· العلم: `THM{header_leak}` – قيمة النقاط: +100

# ---
📂 Room 16 – Local File Inclusion (Easy Version)

· تطبيق ويب على `http://localhost:4040` يخدم endpoint `/view?page=...` لتحميل ملفات محلية.
· يمكن استغلال path traversal (مثلاً `../secret/flag.txt`) للوصول إلى العلم المخزن خارج مجلد القوالب.
· المهارة: LFI basic، التلاعب بالبارامترات، فهم تضمين الملفات.
· العلم: `THM{lfi_easy_path}` – قيمة النقاط: +200

# ---
📱 Room 17 – Open ADB Port (Easy++)

· محاكاة جهاز أندرويد يفتح منفذ ADB (5555) بدون مصادقة.
· الاتصال عبر nc بالمنفذ واستخدام الأمر `shell` يعطي صلاحيات تنفيذ أوامر.
· الأمر `cat /sdcard/flag.txt` يكشف العلم.
· المهارة: فحص المنافذ، التعرف على خدمة ADB، استغلال ADB المفتوح.
· العلم: `THM{adb_op3n_d00r}` – قيمة النقاط: +200

# ---
☠️ Room 18 – Final Easy Level Exam: NeoCorp Internal Portal

· محاكاة اختبار اختراق متكامل: الاستطلاع، تحليل JS، تسريب معلومات، وتخمين منطقي.
· لا توجد تلميحات مباشرة؛ يجب بناء سلسلة الأدلة للوصول إلى صفحة الدخول المخفية.
· العلم: `THM{basic_recon_chain_complete}` – قيمة النقاط: +300

