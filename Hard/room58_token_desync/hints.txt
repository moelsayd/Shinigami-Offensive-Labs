[Hint 1] Check /config/settings.json for the JWT secret and the GraphQL endpoint.
[Hint 2] Send a POST to /graphql with {"query":"{ __schema { types { name fields { name } } } }"} to see the available fields.
[Hint 3] Query the flag: {"query":"{ user { flag } }"} with Authorization: Bearer <your_token>.
