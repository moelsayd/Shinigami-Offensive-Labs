[Hint 1] The GraphQL introspection reveals a mutation `updateUserRole` that doesn't check authorization.
[Hint 2] After becoming admin, use `createTask` mutation with a command prefixed "PICKLE:" followed by hex-encoded pickle payload.
[Hint 3] The SSRF at `/proxy?url=` blocks localhost but allows alternative representations like `127.1` or `0x7f000001`. Use it to reach port 10102.
[Hint 4] The worker at `/process` accepts pickle serialized objects. Craft a payload to read `/root/flag.txt`.
