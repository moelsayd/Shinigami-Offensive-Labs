[Hint 1] The JWT token uses a weak secret. Try kid=/dev/null and an empty signature to become admin.
[Hint 2] The bcrypt hash can be cracked with hashcat -m 3200 or john --format=bcrypt.
[Hint 3] In the ADB shell, the `find` command has the SUID bit set. Use it to read the flag file.
